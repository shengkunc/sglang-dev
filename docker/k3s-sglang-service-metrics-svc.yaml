apiVersion: v1
kind: Service
metadata:
  name: sglang-engine
  namespace: sglang
  labels:
    engine: sglang
spec:
  selector:
    engine: sglang
  ports:
    - name: http
      port: 30000
      targetPort: 30000

