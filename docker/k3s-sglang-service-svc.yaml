apiVersion: v1
kind: Service
metadata:
  name: sglang-engine
  namespace: sglang
  labels:
    app: sglang-engine
spec:
  selector:
    app: sglang-engine
  ports:
    - name: http
      port: 30000
      targetPort: 30000

